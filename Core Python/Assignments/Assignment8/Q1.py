#### With paasing parameters (w input)
#### With returning values  (w output)

def rect_area(l, b):
    area = l * b

    return area         # With returning value

l = float(input("Enter the length of the rectangle: "))
b = float(input("Enter the breadth of the rectsngle: "))
print(rect_area(l, b))